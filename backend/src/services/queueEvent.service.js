import { getQueueEvents } from './queue.service.js';

/**
 * SmartProcure Queue Event Service (Phase 2)
 *
 * Provides audit event fetching by delegating to the core queue service.
 */
export { getQueueEvents };
export default {
  getQueueEvents
};
